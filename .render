services:
  - type: web
    name: ort-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    autoDeploy: true
    envVars:
      - key: API_TOKEN
        sync: false
